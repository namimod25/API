import { Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const updateProfile = () => {
  return (
   <SafeAreaView>
             <Text className='text-red-500 text-center'>Update profile</Text>
    </SafeAreaView>
  )
}

export default updateProfile