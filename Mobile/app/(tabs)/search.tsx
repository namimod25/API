import { Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const searchDisplay = () => {
  return (
    <SafeAreaView>
          <Text className='text-red-500 text-center'>search screen</Text>
    </SafeAreaView>
  )
}

export default searchDisplay